# Host Parts
a) Navigate via path
b) navigate via separate component microfrontend-host-part (nested router-outlet)
    create injection context
    navigate via hint for empty url

# View Parts
[] Title not passed via navigation but via handle


Handles thinktank:
ActivatedMicrofrontend:
    capability
    params
