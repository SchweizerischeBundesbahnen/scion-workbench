/**
 * Specifies the name of the optional path or query parameter for a remote URL used in {RemoteSiteComponent}.
 */
export const WB_REMOTE_URL_PARAM = 'wb.remote-url';

/**
 * Specifies the name of the optional path or query parameter for the view title.
 */
export const WB_VIEW_TITLE_PARAM = 'wb.title';

/**
 * Specifies the name of the optional path or query parameter for the view heading.
 */
export const WB_VIEW_HEADING_PARAM = 'wb.view-heading';

/**
 * Associate a route with a reuse identity (either via routing data, or via matrix URL parameter)
 * to not destroy the component upon deactivation.
 */
export const WB_ROUTE_REUSE_IDENTITY_PARAM = 'wb.route-reuse-identity';
