/**
 * Specifies files to be linted in addition to the files specified in `tsconfig.app.json`.
 *
 * Note not to set the `createDefaultProgram` option, as this would cause significant performance degradation.
 */
{
  "extends": "./tsconfig.app.json",
  "include": [
    "src/environments/*.ts"
  ]
}
